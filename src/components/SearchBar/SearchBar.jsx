import { useState } from 'react';
import './SearchBar.scss'

export default function SearchBar({array,handleSearch}) {

    const [search, newSearch] = useState('')
    const [input, newInput] = useState('')
	return (
		<div className='searchBar'>

 		</div>
	);
}